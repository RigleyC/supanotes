import 'dart:io';
import 'package:sqlite3/sqlite3.dart';

void main() {
  final dbPath = 'c:/Users/rigleyc/projects/supanotes/backend/supanotes.db';
  // wait, the flutter app uses a different db.
  // wait, I can check the flutter sqlite db? No, flutter is running on an emulator or windows app.
  // In windows, it's in Documents or LocalAppData.
}
