# Phase 4 Step 4.3 & 4.5 Implementation Plan

> **For agentic workers:** Inline execution.

**Goal:** Implement direct YMap("tasks") observation for reactive task checkbox updates, and legacy doc migration on load.

**Architecture:** Wire YMap("tasks") observation through the existing bridge→coordinator path (4.3); add idempotent schema migration in loadDoc/DocFor (4.5).

**Tech Stack:** Flutter (Dart), Go backend, dart-crdt, ygo/crdt

---

### Task 1: Step 4.3 — Task widget observes YMap("tasks") via bridge

**Files:**
- Modify: `lib/features/notes/domain/yjs_doc_editor_bridge.dart`

Changes:
1. Add `_tasksSub` subscription to observe `YMap("tasks")`
2. In handler, extract `{nodeId: completed}` and call `_coordinator.syncTaskStates()`
3. Also call `_onDocChanged?.call()` for DB projection
4. Initial tasks sync in constructor
5. Cancel `_tasksSub` in `dispose()`

### Task 2: Step 4.5 — Flutter-side legacy doc migration

**Files:**
- Modify: `lib/core/sync/yjs_sync_manager.dart`

Changes:
1. Add `_migrateLegacyDoc(Doc doc)` method
2. Guard: skip if `YMap("tasks")` has entries
3. Check nodes for `type == "task"` with `data.completed != null`
4. Create `YMap("tasks")` entries, remove legacy fields from node data
5. Wrap in `doc.transact()`
6. Call in `loadDoc` after doc initialization

### Task 3: Step 4.5 — Backend-side legacy doc migration

**Files:**
- Modify: `backend/internal/sync/ydoc_service.go`

Changes:
1. Add `migrateLegacyDoc` method
2. Same logic as Flutter side
3. Call in `DocFor` after applying state
