# Cupertino Native UI Migration Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Replace `adaptive_platform_ui` with the Julius pattern: native `cupertino_native_better` controls on Apple platforms and standard Flutter Material/Cupertino controls elsewhere.

**Architecture:** Keep platform decisions at component boundaries. Use `CNTabBar` and `CNPopupMenuButton` on iOS/macOS, `NavigationBar`/`PopupMenuButton` on other platforms, and standard `showCupertinoDialog`/`showDialog` for confirmation. Register Julius-style native navigation observers in the router.

**Tech Stack:** Flutter, GoRouter, Riverpod, `cupertino_native_better ^1.6.0`, standard Flutter Material/Cupertino widgets.

**Spec:** The Julius Flutter implementation in `C:\Users\rigley.costa\Desktop\projetos\julius1\julius_flutter`.

## Global Constraints

- Preserve Android/desktop Material behavior.
- Preserve existing routes, callbacks, labels, and test semantics.
- Do not keep `adaptive_platform_ui` imports after migration.
- Do not add visual-only tests; run focused behavior tests and analyzer.
- Keep `.tmp/` untracked.

---

### Task 1: Dependency and router native integration

**Files:** `pubspec.yaml`, `lib/core/router/app_router.dart`, `lib/shared/widgets/app_navigation_shell.dart`

- [ ] Add `cupertino_native_better: ^1.6.0`, remove `adaptive_platform_ui`, run `flutter pub get`.
- [ ] Register `CNTabBarRouteObserver` and `CNTransitionObserver` on Apple platforms following Julius.
- [ ] Replace `AdaptiveScaffold` with `Scaffold(extendBody: true)` and implement an iOS `CNTabBar` plus non-Apple `NavigationBar`.
- [ ] Run router/navigation tests.

### Task 2: Shared dialogs and menus

**Files:** `lib/shared/widgets/confirm_dialog.dart`, task/notes menu widgets, note card, editor app bar.

- [ ] Use `CNPopupMenuButton`/`CNPopupMenuItem` with `CNSymbol` on Apple platforms.
- [ ] Use `PopupMenuButton`/`PopupMenuItem` on other platforms.
- [ ] Replace adaptive confirmation dialog with `showCupertinoDialog` + `CupertinoAlertDialog` on Apple and `showDialog` + `AlertDialog` elsewhere.
- [ ] Preserve destructive/cancel semantics and callbacks.

### Task 3: Native buttons and platform checks

**Files:** `lib/app/supa_notes_app.dart`, settings/MCP/editor screens, spacing docs.

- [ ] Replace `PlatformInfo` with `defaultTargetPlatform` checks where needed.
- [ ] Replace adaptive copy/focus buttons with standard `IconButton`/`CupertinoButton` or `CNButton` only where Julius has a direct equivalent.
- [ ] Remove obsolete iOS26 adaptive padding assumptions.

### Task 4: Verification

**Files:** focused tests and lockfile generated by pub.

- [ ] Run focused router, task, notes, dialog, and menu tests.
- [ ] Run `flutter analyze` on changed files and `git diff --check`.
- [ ] Confirm `rg "adaptive_platform_ui" lib test pubspec.yaml` returns no matches.
